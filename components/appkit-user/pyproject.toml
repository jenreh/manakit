[project]
name = "appkit-user"
version = "1.14.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Jens Rehpöhler" }]
requires-python = ">=3.13"
keywords = [
    "reflex",
    "mantine",
    "ui",
    "components",
    "web",
]
classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Software Development :: User Interfaces",
]
dependencies = [
    "greenlet>=3.5.5",
    "appkit-commons",
    "appkit-ui",
    "fastapi>=0.141.1",
    "requests_oauthlib>=2.0.0",
    "urllib3>=2.7.0",
    "jinja2>=3.1.6",
]

[project.optional-dependencies]
azure = ["azure-communication-email>=1.1.0"]
resend = ["resend>=2.42.0"]

[project.urls]
Homepage = "https://github.com/jenreh/appkit"
Documentation = "https://github.com/jenreh/appkit/tree/main/docs"
Repository = "https://github.com/jenreh/appkit"
Issues = "https://github.com/jenreh/appkit/issues"


[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
appkit-commons = { workspace = true }
appkit-ui = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [".cache", "__pycache__"]

[tool.coverage.run]
branch = true
source = ["src/appkit_user"]
omit = [
    "*/tests/*",
    "*/components/*.py",
    "*/templates/*.py",
    "*/pages.py",
    "*/roles.py",
]

[tool.coverage.report]
fail_under = 80
show_missing = true

[tool.hatch.build.targets.wheel]
packages = ["src/appkit_user"]
include = [
    "/src/appkit_user/authentication/templates/*.html",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
